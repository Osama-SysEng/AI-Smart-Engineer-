# ملاحظات الإصدار — AI-Smart-Engineer

## نطاق الإصدار

يستكمل هذا الإصدار المكونات التي كانت ناقصة في تكامل SAP، وتوجيه مزودي النماذج اللغوية، وإدارة متغيرات البيئة، مع إضافة اختبارات تكامل وضوابط تشغيل آمن. صُممت التكاملات الخارجية بحيث تبقى مغلقة أو في وضع المحاكاة إلى أن تُضبط أسرار حقيقية، وبيئة موثوقة، وقائمة السماح الخاصة بالنشر.

## التغييرات الرئيسية

| المجال | ما تم إنجازه |
|---|---|
| تكامل SAP | إضافة محول OData/REST حقيقي يعتمد OAuth2 Client Credentials، ويفرض HTTPS وقائمة السماح، ويتحقق من الاستجابات، ويفشل مغلقًا عند غياب إعدادات الإنتاج أو عند طلب وضع محاكاة غير مصرح به. |
| مزودو LLM | جعل اختيار المزود والنموذج ومحددات التكلفة والإعدادات الخاصة بـ OpenAI وAnthropic وGoogle وDeepSeek وLocal معتمدًا على متغيرات البيئة بدل القيم المضمنة في الشيفرة. |
| إدارة البيئة | إضافة `.env.example` شامل لا يحتوي أسرارًا، وأداة `tools/init-env.py` لإنشاء أسرار تطوير محلية عشوائية دون نسخ أسرار إلى المستودع. |
| الاختبارات | إضافة اختبارات تكامل لإعدادات SAP والأمان وتوجيه LLM، وتصحيح توقعات الطلبات غير الموثقة لتستخدم HTTP 401 المتسق مع معيار Bearer authentication. |
| التحقق الساكن | نجاح فحص ترجمة Python لمجلدات API ومجموعات الاختبار، وفحص عدم وجود ملفات `.env` محلية أو مفاتيح معروفة داخل نسخة المصدر. |
| التشغيل | تثبيت اعتماديات الاختبار اللازمة محليًا: `passlib` و`bcrypt` و`email-validator` و`python-multipart` و`pytest-asyncio`. يجب تثبيت الاعتماديات من ملفات المشروع في بيئة نظيفة قبل النشر. |

## نتائج التحقق

تم تشغيل الاختبارات من `apps/api` باستخدام `PYTHONPATH=.` وكانت النتيجة:

> **33 passed, 34 warnings in 5.53s**

كما نجح `python3 -m compileall` على `apps/api/src` و`apps/api/tests`. التحذيرات الحالية غير مانعة للإصدار، لكنها تشمل استعمال إعدادات Pydantic class-based القديمة، و`datetime.utcnow()` في مكتبات أو مسارات محددة، وتحذيرًا متعلقًا بتركيب event loop المخصص في إعدادات الاختبار. ينبغي معالجتها في دورة صيانة لاحقة قبل ترقية Pydantic أو pytest-asyncio الرئيسية.

## متطلبات التفعيل قبل الإنتاج

لا تُستخدم قيم `.env.example` كأسرار. أنشئ نسخة محلية بواسطة `tools/init-env.py` للتطوير فقط، واستعمل مدير أسرار مؤسسيًا في staging والإنتاج. يجب تزويد `SECRET_KEY` بقيمة صريحة لا تقل عن 32 محرفًا، وضبط `CORS_ORIGINS` على نطاقات محددة، وتفعيل TLS لقاعدة البيانات المُدارة، وتعبئة بيانات SAP وLLM من مخزن أسرار خارج المستودع.

يجب إبقاء `SAP_MOCK_MODE` وخصائص التكامل التجريبية في وضع آمن إلى أن تُراجع هوية الخدمة، ونطاقات OAuth2، وقائمة المضيفين المسموحين، ومهلات الاتصال، وإجراءات تدوير الأسرار. لا يمثل نجاح الاختبارات المحلية اعتمادًا للإنتاج ولا يثبت صلاحية بيانات SAP أو نماذج LLM الحقيقية.

## التشغيل المحلي المقترح

بعد مراجعة `.env` محليًا، شغّل الخدمات عبر Docker Compose باستخدام الإعدادات الواردة في README، ثم تحقق من endpoint الصحة والسجلات دون طباعة الأسرار. لا تُرسل ملفات `.env` أو رموز الوصول أو قواعد البيانات المحلية إلى الأرشيف أو أنظمة التذاكر.

## ملفات التحقق المحفوظة خارج حزمة المصدر

تم حفظ سجل الاختبارات في `pytest_verified.txt` ونتيجة الفحص الساكن في `static_scan.txt` داخل مجلد التدقيق الخارجي. لا تُدرج هذه السجلات تلقائيًا في حزمة المصدر النهائية إذا كانت تحتوي مسارات محلية أو تفاصيل بيئة التنفيذ.

## ملاحظات التوافق

يتطلب تشغيل API إصدار Python مدعومًا، وFastAPI، وPydantic Settings v2، وSQLAlchemy Async، وموفر قاعدة البيانات المحدد في `apps/api/requirements.txt`. في بيئة اختبار جديدة، ثبّت اعتماديات المشروع ثم شغّل الاختبارات من مجلد API؛ لا تعتمد على حزم مثبتة عابرًا في جهاز التدقيق.
